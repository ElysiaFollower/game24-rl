# Use a first-pass SFT set of about 10k short success traces

The first SFT milestone will generate roughly eight short success traces per training puzzle, for about 9k-10k training examples. This is a fast result-discovery dataset, not the final or maximum data scale; if the pipeline is correct and the initial score is promising, increasing traces per puzzle or adding controlled trace variants remains an expected path to further improvement.
