# Use a lightweight package structure

We will build a lightweight but disciplined Python package structure with `src/`, `tests/`, `configs/`, `scripts/`, and focused docs rather than a pile of temporary scripts or a heavy framework. This keeps the repository easy to understand during fast iteration while giving future development enough structure to grow into a publishable, reproducible project.
